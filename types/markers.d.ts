export interface MarkerColorConfig {
  name: string; // label shown to user, e.g. 'Bisque'
  color: string; // actual CSS color, e.g. '#cc44aa' or 'transparent'
  onColor: string; // CSS color of text on top of color
}
