import { test, expect } from 'vitest';

test('1 + 1 equals 2', () => {
  expect(1 + 1).toBe(2);
});

test('Hello string equality', () => {
  expect('hello').toBe('hello');
});

// import { TroffClass } from '../scriptTroffClass.mjs';

